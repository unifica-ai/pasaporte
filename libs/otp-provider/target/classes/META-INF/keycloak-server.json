{
    "providers": [
        "classpath:${project.build.finalName}.jar"
    ],
    "spi": {
        "myCustomSpi": {
            "provider": "myCustomProvider",
            "enabled": true
        }
    }
}
